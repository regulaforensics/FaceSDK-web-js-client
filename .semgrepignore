# Ignore mutable GitHub Actions tag findings.
# We intentionally use version tags (e.g. @v4) instead of pinned SHAs —
# digest pinning is managed by Renovate with the pinDigests option.
# Rule: yaml.github-actions.security.github-actions-mutable-action-tag
.github/workflows/
.github/actions/
renovate.json

# Ignore workflow-level env secret scope findings.
# FACEAPI_SLACK_CHANNEL_ID and FACEAPI_SLACK_WORKSPACE_ID are non-sensitive
# channel/workspace IDs (not tokens) and are used across all jobs in the workflow.
# Rule: yaml.github-actions.security.gha-workflow-env-secret
.github/workflows/faceapi-terragrunt-plan.yaml
